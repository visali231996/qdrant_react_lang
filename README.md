uses different embedding models like BGE Small (bge-small-en-v1.5),MiniLM (all-MiniLM-L6-v2), OpenAI Embeddings (text-embedding-3-small), different vector db like Qdrant (Localhost)","FAISS (In-Memory)","Chroma (Persistent)", different rerankers like cohere and bge...
